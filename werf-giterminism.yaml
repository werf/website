giterminismConfigVersion: 1
config:
  stapel:
    git:
      allowBranch: true
  secrets:
    allowEnvVariables:
      - WERFIO_GITHUB_TOKEN
helm:
  allowUncommittedFiles:
    - .helm/trdl_channels.yaml