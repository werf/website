{% include snippetcut_example path=".helm/templates/deployment.yaml" syntax="yaml" snippet="env" examples=page.examples %}

We have defined the log level environment variable. We have also set it to the default `info` level. Note that `pino` does this by default:

{% include snippetcut_example path="app.js" syntax="javascript" snippet="logger-usage" examples=page.examples %}
