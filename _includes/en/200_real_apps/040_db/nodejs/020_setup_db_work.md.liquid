{% include snippetcut_example path="config/database.json" syntax="json" examples=page.examples %}

Sequelize assumes that connections for all environments are defined in a single file. For now, the production environment's data are hardcoded. 
However, we will fix this in future chapters of the guide.
