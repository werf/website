To avoid this, we have to configure the application to write logs in JSON format. Let's create a uwsgi config and define the desired log format in it:

{% include snippetcut_example path="app/uwsgi.ini" syntax="ini" examples=page.examples %}
