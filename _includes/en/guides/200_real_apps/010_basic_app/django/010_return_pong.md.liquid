This path will be handled by the `ping` standard controller method, which returns the `pong` text message:

{% include snippetcut_example path="app/ping/views.py" syntax="python" examples=page.examples %}
