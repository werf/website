{% include snippetcut_example path="src/main/resources/application.properties" syntax="properties" snippet="minio" examples=page.examples %}

> We will leave the connection parameters as they are for now (instead of putting them into environment variables) — in the next chapter, we will discuss a more elegant and proper way of doing this

Create a configuration class, pass the `application.properties` to it and activate MinIO:

{% include snippetcut_example path="src/main/java/io/werf/werfguidesapp/config/MinioConfig.java" syntax="java" examples=page.examples %}

Now, create a component for handling files in MinIO by adding the two main methods, `putObject` and `getObject`, to it:

{% include snippetcut_example path="src/main/java/io/werf/werfguidesapp/components/MinioComponent.java" syntax="java" examples=page.examples %}
