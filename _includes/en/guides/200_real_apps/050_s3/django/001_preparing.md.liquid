## Preliminary steps

Django usually takes advantage of the boto3 library to work with S3.

Let's add it to the project by including the install command in the Dockerfile:

{% include snippetcut_example path="Dockerfile" syntax="python" snippet="install-boto3-import" examples=page.examples %}
