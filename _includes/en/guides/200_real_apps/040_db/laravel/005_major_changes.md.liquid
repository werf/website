We have migrated the configuration for MySQL from the [official Laravel repository](https://github.com/laravel/laravel). The following changes have been made to our application:

1. Adding `mysql-client` and the `pdo_mysql` PHP module to the [Dockerfile]({{ page.base_url | append: page.examples | append: "/Dockerfile" }}).
