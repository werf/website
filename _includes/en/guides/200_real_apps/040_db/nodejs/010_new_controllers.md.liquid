{% include snippetcut_example path="routes/talkers.js" syntax="javascript" examples=page.examples %}

The following command was used for generating a model and a migration:

```shell
npx sequelize-cli model:generate --name Talker --attributes answer:string,name:string
```

{% include snippetcut_example path="db/models/talker.js" syntax="javascript" examples=page.examples %}
