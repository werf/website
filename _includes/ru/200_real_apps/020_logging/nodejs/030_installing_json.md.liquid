Для этого нужно использовать логгер, который подерживает вывод в JSON. Для примера возьмем `pino` и
заодно удалим логгер, который используется по умолчанию.

```shell
$ npm uninstall morgan
$ npm install pino express-pino-logger
```

Подключим логгер к серверу

{% include snippetcut_example path="app.js" syntax="javascript" snippet="logger-import" examples=page.examples %}

{% include snippetcut_example path="app.js" syntax="javascript" snippet="logger-usage" examples=page.examples %}

Библиотека `express-pino-logger` добавила доступ к `pino` через объект ответа: `res.log`. Так логгер можно использовать на более глубоком уровне.

{% include snippetcut_example path="routes/ping.js" syntax="javascript" snippet="logger-usage" examples=page.examples %}