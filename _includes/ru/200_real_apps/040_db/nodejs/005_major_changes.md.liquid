1. Установлены зависимости для работы с MySQL
1. Добавлена конфигурация доступа к БД
1. Добавлена модель и миграция схемы БД
1. Добавлены маршруты, использующие новую модель

Новые зависимости были установлены командой:

```shell
npm i mysql2 sequelize sequelize-cli express-async-handler
```

- `mysql2` — драйвер работы с MySQL
- `sequelize` — ORM для работы с базой данных
- `sequelize-cli` — командная утилита для генерации моделей и миграции
- `express-async-handler` — обертка над обработчиками запросов, чтобы писать код с async/await

Для sequelize мы добавили конфигурационный файл, который определяет структуру файлов sequelize и
конфигурацию подключения к базе. Мы определим все связанные с БД каталоги в каталог `db`, а
конфигурацию положим в `config/database.json`:

{% include snippetcut_example path=".sequelizerc" syntax="javascript" examples=page.examples %}
