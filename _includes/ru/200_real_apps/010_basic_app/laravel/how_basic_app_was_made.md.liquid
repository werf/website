Чтобы его получить, сначала был сгенерирован скелет нового {{ page.framework_name }}-приложения командой:
```shell
docker run --rm \
    -v $(pwd):/opt \
    -w /opt \
    laravelsail/php80-composer:latest \
    bash -c "laravel new example-app"
```
В нём была оставлена конфигурация только для production-окружения и убрано всё, что не требуется на данном этапе.

После этого был добавлен новый путь `/ping` для нашего API:
{% include snippetcut_example path="routes/web.php" syntax="php" examples=page.examples %}

Этот путь будет обрабатываться методом `ping` стандартного контроллера, возвращающим текст `pong`:
{% include snippetcut_example path="app/Http/Controllers/PingController.php" syntax="php" examples=page.examples %}
