Этот путь будет обрабатываться методом `ping` стандартного контроллера, возвращающим текст `pong`:

```
TODO (_includes/ru/200_real_apps/010_basic_app/framework_id/010_return_pong.md.liquid):  метод `ping` стандартного контроллера, возвращающий текст `pong`.
```