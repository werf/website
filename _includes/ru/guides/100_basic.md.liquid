В этом разделе мы покажем, как с помощью werf собрать образ и развернуть его в Kubernetes.

В качестве тестового приложения будет использоваться небольшой shell-скрипт, имитирующий простейшее приложение, которое возвращает `pong` при обращении на endpoint `/ping`. В следующем разделе мы поменяем этот shell-скрипт на простое приложение на {{ page.framework_name }}, в которое мы будем добавлять новую функциональность в каждой новой главе, в конечном итоге получив приложение, напоминающее реальное.
